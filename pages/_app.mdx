
{/* TODO: Remove, just for debuging while w are migrating away from tailwind */}
import "nextra-theme-docs/style.css";
import "./tmp.css";

{/* _app.mdx is not typesafe but _app.tsx is. */}
{/* _app.mdx is used in nextra for better performance and smallest bundle size. */}
export { default } from "@codegouvfr/nextra-theme-dsfr/_app";